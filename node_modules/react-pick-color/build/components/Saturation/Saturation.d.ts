/// <reference types="react" />
import { HslColor, HsvColor } from "../../types";
export declare type SaturationProps = {
    hsl: HslColor;
    onChange?: (color: HsvColor) => void;
};
declare const _default: ({ hsl, onChange }: SaturationProps) => JSX.Element;
export default _default;
